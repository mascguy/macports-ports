--- contrib/libev/CMakeLists.txt.orig	2023-07-01 15:12:33.000000000 -0400
+++ contrib/libev/CMakeLists.txt	2023-07-01 15:18:18.000000000 -0400
@@ -79,4 +79,12 @@
 	ADD_DEFINITIONS(-DEV_VERIFY=3)
 ENDIF()
 
+IF(ENABLE_EV_USE_MONOTONIC MATCHES "ON")
+	MESSAGE(STATUS "ENABLE_EV_USE_MONOTONIC: enabled")
+	ADD_DEFINITIONS(-DEV_USE_MONOTONIC=1)
+ELSE()
+	MESSAGE(STATUS "ENABLE_EV_USE_MONOTONIC: disabled")
+	ADD_DEFINITIONS(-DEV_USE_MONOTONIC=0)
+ENDIF()
+
 INSTALL(TARGETS rspamd-ev LIBRARY DESTINATION ${RSPAMD_LIBDIR})
